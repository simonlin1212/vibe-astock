<p align="center"><b>简体中文</b> | <a href="README_en.md">English</a></p>

<h1 align="center">Vibe-Astock</h1>

<p align="center">
  <b>A 股短线复盘看板 —— 打开就看清今天的短线情绪</b><br>
  派生情绪指标 · 全本地运行 · 可用本机 CLI 订阅免 key
</p>

<p align="center">
  <img src="https://img.shields.io/badge/license-Apache--2.0-blue.svg" alt="License">
  <img src="https://img.shields.io/badge/python-3.12-3776AB.svg?logo=python&logoColor=white" alt="Python">
  <img src="https://img.shields.io/badge/react-19-61DAFB.svg?logo=react&logoColor=white" alt="React">
  <img src="https://img.shields.io/badge/tests-533%20passing-brightgreen.svg" alt="Tests">
  <img src="https://img.shields.io/badge/version-v0.2.1-orange.svg" alt="Version">
</p>

<p align="center">
  <a href="#这是什么">这是什么</a> ·
  <a href="#界面">界面</a> ·
  <a href="#当前版本包含">当前版本</a> ·
  <a href="#核心派生情绪指标">派生情绪指标</a> ·
  <a href="#架构">架构</a> ·
  <a href="#自定义分析口径prompt-包">自定义口径</a> ·
  <a href="#快速开始">快速开始</a> ·
</p>

<p align="center">
  <b>⚠️ 本项目只做公开盘面数据的整理、统计与市场层面研判，不推荐个股、不预测涨跌、不给买卖时机。<br>
  不构成任何投资建议，也不提供任何投资服务。</b>
</p>

---

## 寻找深圳的 AI 工作机会

作者目前正在寻找深圳的 AI 相关工作机会，重点关注腾讯等大型科技企业及金融机构的 **AI 投研产品、FDE 与 AI 咨询 / 解决方案岗位**。

兼具金融机构从业经历与 AI 产品实战，持续构建金融市场数据工具和多智能体系统，开源项目累计获得 **17K+ GitHub Stars**。

联系：[simonlin0423@gmail.com](mailto:simonlin0423@gmail.com)

---

## 这是什么

**帮你把今天的复盘做完。**

短线复盘每天要翻涨停池、连板梯队、龙虎榜、板块资金、题材归因，再算一遍昨日涨停股今天赚不赚钱。
这套东西每天重复一小时。Vibe-Astock 把它自动化：拉数据 → 五个分析师各读一面 →
收敛成一份能读的盘面研判。

复盘本身是**事实工作**（把今天发生的事整理清楚），本来就不需要推荐。
所以屏幕上九成是**硬指标层**——赚钱效应 / 晋级率 / 梯队结构 / 情绪周期 / 亏钱效应 /
封板质量 / 题材事件树 / 历史统计位置——**全部纯计算、数据源直出，不经过 AI，打开就有**。
AI 的角色是把七八个数据源串成一个能读的故事，不是选股。

**它不做什么**：不推荐个股、不给参与倾向、不给买卖点位。个股一律只作客观陈述
（属于哪个题材、几板、龙虎榜谁在买、技术位置如何）。方向与情绪判断做到板块层面为止。

---


## 界面

### 看市场

**复盘看板** —— 打开就有的硬指标层。全市场宽度当分母，昨日强势股今天落到哪一档，赚钱效应与亏钱效应对着看。

<img src="assets/screenshots/01-review-metrics.png" alt="复盘看板：全市场宽度 / 昨日强势股反馈 / 赚钱效应 / 亏钱效应">

**明日验证条件** —— 把今晚的判断变成明天能对账的东西：每条都带今日基准值和"变动超过多少才算数"的阈值。

<img src="assets/screenshots/02-verification.png" alt="明日验证条件：每条带今日基准值与阈值">

**涨停样本统计** —— 同样是首板，什么时候把板最终封住，次日期望差出一大截。⚠️ 页面顶部固定写着样本偏差：这是「事后知道封住了」的名单，真实期望必然更低。

<img src="assets/screenshots/07-backtest.png" alt="涨停样本统计：封板时间曲线与样本偏差声明">

**盘面数据** —— 指数、隔夜外围（含美股七姐妹）、板块资金、成交额榜、今日实时打板情绪，可开自动刷新。

<img src="assets/screenshots/03-market-data.png" alt="盘面数据：大盘指数与隔夜外围">

**近5天热度 + 龙头谱系** —— 五个交易日的情绪走向，以及前几天的龙头现在回撤到什么位置。

<img src="assets/screenshots/04-heat.png" alt="近5天情绪热度与龙头谱系">

### 看自己

> 下面两张里的交易记录是**示例数据**，只为展示界面。你的账本只存在本机。

**交易日志** —— 每笔交易自动钉上当时的市场环境，再按情绪环境 / 打法 / 是否按计划 / 当时板位 / 持有周期五个维度分组自查。样本少的时候明确标出来。

<img src="assets/screenshots/05-journal.png" alt="交易日志：记一笔与五维度分组自查">

**在险资金** —— 按你自己写下的计划止损算，看的是加总。没写止损的仓位单独列出、不当零计入，所以那个合计写的是「下限」。

<img src="assets/screenshots/06-at-risk.png" alt="在险资金：有边界的合计在险与未设边界的仓位">

### 按需分析

**个股深挖** —— 四个分析师 + 正反两方 + 裁判。只给客观描述与风险披露，不给参与倾向。

<img src="assets/screenshots/08-deepdive.png" alt="个股深挖：题材 / 资金 / 技术三栏与风险披露">

---

## 当前版本包含

**v0.2.0** —— 侧栏十四个入口：

| 入口 | 是什么 |
|---|---|
| **复盘看板** | 主体，默认落地页。AI 叙述在页顶，硬指标在下方 |
| 盘面数据 | 指数 / 板块资金 / 资金轮动 / 热点个股 |
| 首板分析 | 当日首板池与封板结构 |
| 近5天热度 | 近 5 个交易日的题材热度变化 |
| **盯盘** | 交易时段实时刷新的异动流：急拉急跌、封板开板，只列客观事件 |
| **持仓股** | **交易日志的视图**（不另存一份账）；行情取不到时明确标注，不显示成归零 |
| **自选股** | 自己关注的标的，与盯盘页共用同一份本地数据 |
| **个股数据** | 单只标的的行情、估值、财报快照与研报列表 |
| **资讯雷达** | 多源新闻聚合 |
| **盘中核验** | 09:25 竞价核验 + 盘中六个时点的情绪路径，后台自动抓 |
| **个股深挖** | 按需跑的单股 agent：四个分析师 + 正反两方辩论 + 裁判，只作客观陈述 |
| **涨停样本统计** | 昨日涨停样本在次日的历史表现，分情绪环境；含原始数据归档与结构漂移检测 |
| **交易日志** | 记自己的每一笔并附上当时的市场环境；分组自查 + 账户风险与执行偏差 |
| 接入 AI | 配 API key 或本机 CLI（见下） |

### 盯盘与自选（新增）

- **盯盘** —— 交易时段每 3 秒刷新一轮（L1 快照的极限频率），把急拉急跌、封板开板这类
  **客观事件**串成一条异动流；同一只票同类事件 5 分钟内只报一次。非交易时段自动停。
- **持仓股 / 自选股** —— 自己录入，数据只存本机。⚠️ 行情取不到时后端会给 `price=0`，
  那会让界面显示成「持仓全部归零、亏损 100%」——**完全是假的**。所以逐行判断行情可用性、
  如实标注"行情不可用（不是真的归零）"，汇总数字也一并标成不完整；
  **喂给 AI 的上下文同样过这道识别**，否则一问 AI 就会得到一份基于"全部亏光"的分析。
- **个股数据 / 资讯雷达** —— 单只标的的行情估值财报，以及多源新闻聚合。

榜单（连板股、成交额等）都是客观公开数据，只呈现事实，不推荐个股、不预测涨跌、不给买卖时机。

### 盘中核验（新增）

只做盘后复盘的工具，第二天早上就和你断线了：昨晚判了「退潮」，今早到底应验没有？
这一页补两件事：

- **09:25 竞价核验** —— 昨日涨停股整体高开还是低开、各板位竞价强弱、最高标什么状态、
  昨晚立下的验证条件有没有出现苗头。
- **盘中情绪路径** —— 09:25 / 09:35 / 10:00 / 11:30 / 14:00 / 15:00 六个时点各存一张快照。
  同样是收盘 40 家涨停，早上 60 家炸到 40 家、和早上 15 家扩散到 40 家，含义完全相反 ——
  只有留下路径才看得出来。

⚠️ **快照过点就补不回来**（拿的是实时行情），所以由后台线程按点自动抓，不等人想起来点一下。
⚠️ 更要紧的是：**过了那个点，系统不会拿此刻的实时值冒充当时的快照** ——
它会明说"09:25 快照缺失、现在距那个时点已超 8 分钟"，而不是给你一个看着正常的假数字。
调度按上海时区判断，机器放在别的时区也照常工作。

本页只产出市场层面的聚合读数（高开/低开家数、各板位均值、题材前排整体强弱），
个股一律只列客观数据，不排序、不评分、不给参与倾向。

### 个股深挖（新增）

前面几个入口讲的是整个盘面，这一个针对**单只标的**、按需跑一次（约 2-3 分钟）：
题材归属 / 资金流向 / 技术形态 / 风险排查四个分析师并行给报告 →
正方与反方各自论证 → 裁判汇总成一份结论，结论下方可以继续追问。

⚠️ **边界与复盘一致，而且更严**：只作客观陈述与风险披露，不给参与倾向
（"值得关注/建议回避"这类）、不给买卖点位或时机。这条口径写在 prompt 包里，
换成你自己的口径也仍然由 prompt 包统一约束，引擎不硬编码。

两道护栏：连行情都取不到时（停牌 / 无效代码）**直接返回错误，不会白跑七次 LLM**；
从外部抓回的文本一律标为不可信来源，裁判层同样对待。

### 涨停样本统计（新增）

跑几种经典短线打法在最近 20 / 30 / 60 / 90 个交易日的统计：胜率、期望、分布、按日曲线，
以及**分情绪环境**的表现（同一套打法在发酵期与退潮期往往是两回事）。

⚠️ **这是「市场现象统计」，不是策略回测，更不是"打板策略的收益"。** 三者必须分清：

| 层级 | 是什么 | 本项目 |
|---|---|---|
| ① 市场现象统计 | 某类票在历史上的表现 | **就是这一层** |
| ② 用户规则历史模拟 | 按你的规则回放，含成交概率与滑点 | 没做 |
| ③ 用户真实成交统计 | 你实际下单成交的结果 | 见交易日志 |

样本来自「前一日收盘时留在涨停池里的票」——这是**事后知道封住了**的名单，
天然漏掉冲板没封住的、排队没成交的、一字板买不进的。真实打法的期望必然低于这里的数字。
这段披露显示在页面顶部，不折叠。

同页还有两块地基：

- **原始数据归档** —— 每天把数据源**原样返回的内容**存下来、永不删除（一天几百 KB）。
  缓存存的是派生结果、随时可删重建；归档是"口径改了之后还能把历史重算一遍"的唯一依据。
  ⚠️ 存的必须是原始行：归一化过的行既检不出字段漂移，也拿不回被丢掉的列。
- **结构漂移检测** —— 把三件事分开：**数据源变了**（字段清单变化）/ **市场结构变了**
  （板块占比、涨停家数分布的窗口比较）/ **制度变了**（涨跌幅、920 号段、连板口径）。
  不分开就会把前两类读成第三类，然后基于一个假信号调整打法。
  ⚠️ 制度事件**只收人工登记，绝不从数据反推** —— 反推是猜，猜错会把市场波动当成制度事件。

### 交易日志（v0.2.0 新增）

上面四个入口讲的都是「市场今天怎么了」，这一个讲「我自己做得怎么样」。

- **每笔交易自动钉上当时的市场环境** —— 那天的情绪档位、赚钱效应，以及这只票当天几板、
  几点封的板、炸过几次、属于哪个题材。买入日与卖出日的环境**各存一份**。
- **成交明细**（可选）—— 填了就自动算加权成本、已实现盈亏、持有天数，支持分批建仓、
  做 T、隔日卖出。⚠️ 结算按**成交时序逐笔**走：卖出多于当时持仓会直接报错而不是凭空结算；
  平仓后再买入开启新的持仓周期，**已经发生的盈亏不会被后来的买入改写**。
  成本用移动加权平均（与券商对账口径一致），只算已实现部分。
- **盈亏是净额** —— 佣金、印花税、过户费都扣掉了。费用可以逐笔填实际值（对账单上的数），
  也可以配自己的费率让它估算，界面上会标出这个数是估的还是真的。
  ⚠️ 默认费率只是能跑起来的初值，不是推荐值 —— 对高换手的打法，一堆薄利交易在计费后
  可能接近持平甚至转亏，不改成自己的真实费率，胜率与期望会系统性偏高。
- **持仓中的记录可以补成交** —— 买入当天写下计划止损，第二天回来补卖出即可，
  不用删了重录。建仓时间与「计划边界是下单时写的」这个证据都原样保留；
  事后修改计划止损不禁止，但会留下改动时间。
- **计划退出边界** —— 下单时写下的止损价 / 目标价。⚠️ 它只接受当时写下的值，
  事后补填会让这个口径失去意义。
- **分组自查** —— 按情绪环境 / 打法 / 是否按计划 / 当时板位 / 持有周期五个维度分组，
  每组标注样本量，样本少时明确提示别下结论。
- **个人模式卡** —— 把自己的打法写下来并**带版本**：改了规则就开新版本，业绩按版本分段，
  用来回答「上一次调整之后表现有没有变化」。改卡片不会重写历史归属，
  卡片创建之前的交易单独列出、不计入任何版本。

同一页往下还有一组**账户风险与执行偏差**：

- **在险资金** —— 按你自己写下的计划止损算 `(成本 − 止损价) × 股数`，看的是**加总**。
  ⚠️ 没写止损的仓位归入「未设边界」单独报数，**不当 0 计入** —— 否则总在险会被系统性
  低估，而数字看着完全正常。占比的分母只用你自己填的账户规模，没填就只给绝对金额。
- **权益曲线的形状** —— 不看累计盈利，看距高点回撤多深多久、多少笔没创新高、
  以及**去掉最好的 1 笔 / 3 笔之后还剩多少**。
- **滚动窗口** —— 近 10 / 20 / 50 笔与终身并排。笔数不够那个窗口时标灰注明，
  避免「只有 12 笔却报近 50 笔」被当成在那个尺度上验证过。
- **MFE / MAE 与盈利回吐** —— 这笔最多曾赚到多少、最多曾亏到多少、实际吃到几成。
  ⚠️ 日线拿不到盘中时序，MFE 是**上界**、捕获率被系统性低估，这个偏差与结论并排显示。
- **判断 vs 执行归因** —— 把「昨晚那份判断成没成立」与「你那天赚没赚」交叉成四格。
  「判对了却亏钱」是执行问题，「判错了还赚钱」是运气，后者单独点出来。
- **异常交易收件箱** —— 按客观条件筛出少数几笔值得回头看的。
  ⚠️ 判定基准**全部来自你自己**（相对你自己的中位仓位与中位持有天数、你自己写的阈值），
  样本不足时明确不做这类判定；只说哪里不寻常，不说该怎么做。
- **风险宪法** —— 你自己写下的阈值（单笔/单日最大亏损、最大持仓数、连亏降档…）。
  系统只检查有没有违反**你自己的**规则，默认值只是能跑起来的初值，不是推荐值。
  ⚠️ 每条规则都会报「查了没有」：算不出来时（比如没填账户规模就没有单日亏损的分母）
  明确标 `unavailable`，而不是显示成"0 次违反"——**配了却没被检查的规则比没有更危险**。

边界：**只统计你自己录入的历史行为**，不做选股、不给买卖时机、不产出任何下一笔建议 ——
它是记账本与体检报告，不是参谋。数据只存本机，⛔ **不接入任何 AI prompt**
（由测试锁住）。

## 核心：派生情绪指标

涨停家数只是原料。真正决定盘面状态的是这几个**派生**读数：

| 指标 | 说明 | 为什么重要 |
|---|---|---|
| **赚钱效应** | 昨日涨停股今天的均值 / **中位数** / 翻红率 / 再涨停率 | 涨停 40 家但昨日涨停股今天中位 -1.8%，那是退潮；涨停 25 家但中位 +4%，那是健康 |
| **晋级率** | 昨日各档连板今天仍涨停的比例（1进2 / 2进3 / 3板以上） | **1进2 最敏感**：明显走低=退潮，回升=修复 |
| **连板溢价** | 昨日 2 板以上个股今天的表现 | 高标承接度 |
| **梯队结构** | 各档连板家数 + 断层检测 | 有 5 板和 2 板却缺 3-4 板 = 最高标悬空，断板后无下一梯队承接 |
| **情绪周期** | 近 10 个交易日情绪分曲线，定位本轮起点与"第几天" | 知道现在处在周期什么位置 |

> ⚠️ **均值与中位数经常背离**——少数大涨会把均值拉起来。看"多数人的体感"以中位数为准。

---

## 架构

```
涨停池 / 龙虎榜 / 板块资金 / 题材归因 / 腾讯行情
        ↓
派生情绪指标 + 客观事实表（纯计算，不经过 AI —— 屏幕上的九成）
        ↓
五个短线分析师（情绪面 · 资金面 · 题材热点 · 龙虎榜游资 · 龙头跟踪）
        ↓
复盘裁判 → 结构化盘面研判
        （情绪档位 / 活跃方向 + 依据 + 风险证伪条件 / 风险提示）
```


---

## 自定义分析口径（Prompt 包）

引擎（数据管线 / 多 agent 编排 / 反思闭环）是通用的；**说什么、说到什么程度**由 prompt 包决定。
每个人的短线体系不一样——有人看情绪周期，有人只做首板，有人只跟资金——所以这一层做成可替换的。

本仓库自带 `RESEARCH_PACK`（市场与板块层面的观察与研判）。想换一套口径：

最省事的写法 —— 只换措辞，结论 schema 沿用自带的（这段可以直接照抄跑）：

```python
# ~/.vibe-astock/prompts_local.py
from duanxian.prompts import PromptPack, RESEARCH_PACK

PACK = PromptPack(
    name="my-style",
    analyst_style="只讲情绪周期位置和晋级率，别的少说。",   # 五个分析师的语气与尺度
    analyst_len="控制在 250 字内。",
    judge_requirements="""1. 判断当前市场情绪档位（冰点/修复/发酵/亢奋/退潮）。
2. 说清这个档位的依据是哪几个读数。
3. 列出需警惕的风险信号。""",                              # 裁判的产出要求
    # 下面三个是一组，要么全用自带的，要么自己写一整套
    focus_model=RESEARCH_PACK.focus_model,      # = schemas.TomorrowFocus
    focus_skeleton=RESEARCH_PACK.focus_skeleton,
    render_focus=RESEARCH_PACK.render_focus,
)
```

想连结论结构一起换，就自己定义 `focus_model`（pydantic 模型）、
`focus_skeleton`（给 JSON 模式的英文键骨架）和 `render_focus`（模型 → markdown），
三个必须配套。完整字段见 `duanxian/prompts.py` 的 `PromptPack`，
可照 `duanxian/schemas.py` 里 `TomorrowFocus` 的写法改。

引擎会自动发现该文件（也可用环境变量 `VIBE_ASTOCK_PROMPTS` 指向任意路径），加载失败会打印原因并回退默认包。
该文件在本仓库之外、不随代码分发；写什么、怎么用、由此产生的责任由使用者自负，
并请自行确认所在司法辖区对相关活动的资质要求。

---

## 快速开始

```bash
# Python >= 3.10（akshare 新版要求）
python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
```

前端要先构建（构建产物不进 git，由后端直接服务）：

```bash
cd frontend && npm install && npm run build
```

配置 LLM —— **两条后端，有哪个用哪个**：

**① 有 OpenAI 兼容的 API key**（默认，示例是 MiMo）：

```bash
# ~/.config/mimo/mimo.env
MIMO_API_KEY=...
MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
MIMO_MODEL=mimo-v2.5-pro
# 可选：quick 档（分析师等高频节点）用哪个模型，不填就是 mimo-v2.5
# MIMO_QUICK_MODEL=mimo-v2.5
```

不是 MiMo 也行：任何 OpenAI 兼容端点都能接，比如 DeepSeek 就把 `MIMO_BASE_URL` 指到
`https://api.deepseek.com/v1`，`MIMO_MODEL=deepseek-reasoner`、`MIMO_QUICK_MODEL=deepseek-chat`
（两档都要填成那家的模型名，否则 quick 档会按默认的 `mimo-v2.5` 去请求、直接 404）。

**② 只有 Claude / Codex 订阅、没有 API key**：走本机已登录的 CLI，免 key。

```bash
VIBE_LLM_CLI=claude .venv/bin/python server.py
```

> ⚠️ 在 server 上跑 **claude 以外**的 CLI，还要显式加 `VIBE_ALLOW_UNSAFE_CLI=<同一个>`。
> 不自动放开是有意的：那会连「网页里问 AI」一起放开，而那条路会把抓来的外部新闻原文
> 塞进 prompt，注入面比复盘大得多。`claude` 分支带 `--disallowedTools`，做一次性问答够用。
> 用 `python main.py` 独立跑复盘则不需要第二个开关。

<details>
<summary><b>Windows 用户看这里（上面的命令是 macOS / Linux 写法）</b></summary>

Windows 上有两处不一样：解释器路径是 `.venv\Scripts\python`（不是 `.venv/bin/python`），
环境变量不能写在命令前面，要先单独设。

**PowerShell**（推荐）：

```powershell
py -3.12 -m venv .venv
.venv\Scripts\pip install -r requirements.txt

# 前端必须先构建，否则打开页面是 503（frontend/dist 不进 git）
cd frontend; npm install; npm run build; cd ..

# ① 用 API key：照上面写 mimo.env，路径是 $env:USERPROFILE\.config\mimo\mimo.env
# ② 用本机 CLI 订阅：
$env:VIBE_LLM_CLI = "claude"
.venv\Scripts\python server.py
```

**CMD**：

```bat
py -3.12 -m venv .venv
.venv\Scripts\pip install -r requirements.txt

rem 前端必须先构建，否则打开页面是 503
cd frontend && npm install && npm run build && cd ..

set VIBE_LLM_CLI=claude
.venv\Scripts\python server.py
```

要跑 `claude` 以外的 CLI，同样要加第二个开关（PowerShell：
`$env:VIBE_ALLOW_UNSAFE_CLI = "codex"`；CMD：`set VIBE_ALLOW_UNSAFE_CLI=codex`）。

前提是那个 CLI **已经装好并登录过**，在同一个终端里直接敲 `claude` 能进得去——
后端只是去 PATH 里找它、复用它的登录态，不会替你登录。
如果 `claude` 单独能跑、经 server 却报「未检测到」或起不来，
请把完整报错贴到 [issue](https://github.com/simonlin1212/vibe-astock/issues) 里，
注明 Windows 版本和 CLI 的安装方式（npm / 安装包），我们跟进。

</details>

跑起来：

```bash
.venv/bin/python server.py          # 一个进程一个端口 :8910，十四个入口全在
```
```bash
.venv/bin/python main.py            # 或者 CLI 直接跑今天的复盘
```

⚠️ **复盘的对象只能是已经收盘的那一场。** 不带日期时自动取最近已收盘交易日；
指定一个还没收盘的日子会被拒绝并指回那一场（涨停池 / 龙虎榜盘中都还没定稿，
喂进去只会让 AI 硬凑）。那一场已经跑过就直接看，不重跑；要重跑加 `?force=1`。

**历史场次随时能看**：赚钱效应 / 亏钱效应 / 连板溢价 / 昨日强势股反馈都优先走
**定稿记录**（已收盘的日子落盘缓存，否则走东财昨日涨停池），不依赖实时行情 ——
所以盘中也能翻上周三那份复盘。

数据落在 `~/.duanxian-agents/`（复盘 / 热度 / 缓存 / 交易日志 / 模式卡），
盘面数据那几个分栏落 `~/.vibe-research/`。交易日志在 `~/.duanxian-agents/journal/trades.json`
单文件、本地、不上传。

### 环境变量

| 变量 | 默认 | 作用 |
|---|---|---|
| `VIBE_PORT` | `8910` | 后端端口 |
| `VIBE_LLM_CLI` | 未设 | 用本机 CLI 当 LLM（`claude` / `codex` …），设了就不需要 API key |
| `MIMO_QUICK_MODEL` | `mimo-v2.5` | API 模式下 quick 档用的模型名；接 DeepSeek 等别家端点时必填（也可写在 `mimo.env`） |
| `VIBE_ALLOW_UNSAFE_CLI` | 未设 | 放开 `claude` 以外的 CLI，逗号分隔（见上面那条提醒） |
| `VIBE_ASTOCK_PROMPTS` | `~/.vibe-astock/prompts_local.py` | 换一套分析口径（见「自定义分析口径」） |
| `VIBE_ALLOW_HOSTS` | 未设 | 挂到域名下访问时把域名加进来，否则写操作 403 |
| `VIBE_MARKET_PROXY` | 未设 | 东财在你这儿**只能经代理**才连得上时设 `1`。等同于 `VR_DATA_PROXY=1`，设哪个都行 |
| `VIBE_MARKET_DIRECT` | 未设 | 相反方向：代理把东财挂掉、连取涨停池都失败时设 `1` 强行直连。⚠️ 它是**进程级**的，会一并关掉东财请求的代理回退 |
| `VR_API_KEY` | 未设 | 给盘面数据那几个分栏的接口加一层 key 校验 |

---

## 数据源

| 来源 | 提供 | 要 key 吗 |
|---|---|---|
| akshare（东财涨停池 / 龙虎榜） | 涨停 · 炸板 · 跌停 · 连板梯队 · 龙虎榜席位 | 不要 |
| 东财 `push2delay` clist | 板块 / 个股资金流、成交额榜 | 不要 |
| akshare 昨日涨停池 | **定稿记录**：昨日涨停股在目标日的表现（赚钱效应 / 亏钱效应 / 连板溢价 / 反馈矩阵的主来源）| 不要 |
| 腾讯财经 `qt.gtimg.cn` | 实时行情批量（自选股、今日实时打板情绪；也作上面那几项的兜底）| 不要 |
| 腾讯 hist `stock_zh_a_hist_tx` | K 线与交易日历（部分网络下东财 push2his 被封，故走腾讯） | 不要 |
| 同花顺问财 | 涨停原因题材串（→ 题材事件树） | **要** |

**除了题材串，其余全部免费直连、不用任何 key。** 题材串走同花顺问财，需要
`IWENCAI_API_KEY`；不配也能跑，只是「题材事件树」那一块会如实标成不可用
（复盘的其余部分不受影响）。配法：在仓库根建 `.env` 写一行

```
IWENCAI_API_KEY=你的key
```

历史交易日的涨停池摘要会落盘缓存（`~/.duanxian-agents/cache/`）——它们是不会再变的事实，
第二次起几乎零成本。

---

## 测试

```bash
.venv/bin/python -m pytest -q
```

533 个用例，覆盖指标计算、口径边界与降级路径 —— 重点在那些**错了也看不出来**的地方：
界面照常渲染、数字看着合理，但结论是错的。

其中一组专门锁住个人交易数据的边界：账本损坏时必须报错而不是返回空表、并发写入不丢单、
迁移老账本不反推计划止损、录入接口每个字段都要透传、以及**喂 prompt 的模块一律不得引用
个人数据模块**。

---


## 免责声明

> - 本系统产出的所有内容均由 AI 自动生成，可能存在错误或偏差
> - 本项目不构成任何投资建议；投资决策请咨询持有相应资质的专业机构
> - 作者不对使用本工具产生的任何损失承担责任
> - 股市有风险，投资需谨慎

## 赞赏

觉得有用的话，可以请我喝杯咖啡 ☕

<p align="center">
  <a href="https://buymeacoffee.com/simonlin1212"><img src="./assets/bmc-qr.png" width="180" alt="Buy Me a Coffee"></a>
</p>

## License

Apache-2.0，详见 [LICENSE](LICENSE)。

**作者：** Simon 林 · X [@linsizhen](https://x.com/linsizhen) · 邮箱：[simonlin0423@gmail.com](mailto:simonlin0423@gmail.com)
